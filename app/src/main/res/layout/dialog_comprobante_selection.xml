<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Seleccionar Tipo de Comprobante"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="20dp" />

    <RadioGroup
        android:id="@+id/radioGroupComprobante"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <RadioButton
            android:id="@+id/radioBoleta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Boleta"
            android:textSize="16sp"
            android:checked="true" />

        <RadioButton
            android:id="@+id/radioFactura"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Factura"
            android:textSize="16sp"
            android:layout_marginTop="12dp" />
    </RadioGroup>

    <!-- Campos para factura -->
    <LinearLayout
        android:id="@+id/layoutFactura"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:layout_marginTop="16dp">

        <EditText
            android:id="@+id/etRazonSocial"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:hint="Razón Social"
            android:inputType="textCapWords"
            android:padding="16dp"
            android:background="@drawable/edit_text_background"
            android:layout_marginBottom="12dp" />

        <EditText
            android:id="@+id/etRuc"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:hint="RUC"
            android:inputType="number"
            android:maxLength="11"
            android:padding="16dp"
            android:background="@drawable/edit_text_background"
            android:layout_marginBottom="12dp" />

        <EditText
            android:id="@+id/etDireccionFiscal"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:hint="Dirección Fiscal"
            android:inputType="textCapWords"
            android:padding="16dp"
            android:background="@drawable/edit_text_background" />
    </LinearLayout>
</LinearLayout>